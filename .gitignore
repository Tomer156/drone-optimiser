
# local Claude Code settings (permissions, local hooks)
.claude/settings.local.json

# harvested vendor datasheet images (~63MB working cache); the scripts that
# fetch and parse them are kept
.scrape/*
!.scrape/*.py

# generated for the Cloudflare KV upload; the point of moving it is that it is not published
db.json

# wrangler local state
.wrangler/
